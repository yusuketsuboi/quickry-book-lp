# Quickry Book LP 復旧 Phase 1 — AI実行タスク完了報告

訴求軸: 社内向け

- 作成日: 2026-05-10
- 作成者: 佐川睦男（AIエンジニア・LP/Web横断）
- 親計画書: `01_プロダクト/quickry_book_lp/20260509_計画_LP復旧Phase1_佐川睦男.md`
- 関連: `01_プロダクト/技術調査/20260414_LP公開手順書.md`（梶原CTO作成・基礎手順）
- 報告先: 十兵衛CEO・梶原CTO

---

## 0. サマリ（坪井さん向け）

**結論**: AI側で実行可能なタスク（5/5項目）すべて完了。LPコードは `lp-book.quickry.jp` 公開可能な状態。残りは坪井さん実操作6項目（Vercel新規作成・GA4・Route 53等）。

**所要時間（実績）**: 約25分（見積60〜90分の範囲内・コード整合性が完璧で修正不要だったため短縮）

**5/15β版ローンチ前完了見込み**: ◎

---

## 1. 実行結果サマリ

| Step | 内容 | 結果 |
|------|------|------|
| 1 | 旧 `.vercel/` 退避 | ✅ `_archive/01_プロダクト_quickry_book_lp_vercel_old_20260510/.vercel/` へ退避完了 |
| 2 | コード整合性確認 | ✅ 修正不要（5ファイルすべて 4/14 手順書反映済み） |
| 3 | env ファイル雛形作成 | ✅ `.env.example` 作成（実キー含まず） |
| 4 | package.json / Node version 確認 | ✅ Node 20.x 推奨を `@types/node ^20` で裏付け |
| 5 | 報告書作成 | ✅ 本ファイル |

---

## 2. Step 1 — 旧 `.vercel/` 退避（詳細）

### 退避先
```
_archive/01_プロダクト_quickry_book_lp_vercel_old_20260510/
└── .vercel/
    ├── project.json            ← 旧 projectId / orgId メタデータ
    ├── .env.production.local   ← Vercel CLI 内部メタのみ（GA_ID 等の自前 env_var なし）
    ├── README.txt
    └── output/                 ← 過去 build artifact
```

> 計画書では `01_プロダクト/_archive/` を想定したが実体は不在。マイドライブ直下 `_archive/` の既存命名規則（`01_プロダクト_xxx_YYYYMMDD/`）に合わせて配置先を補正した（既存4ディレクトリと整合）。

### 旧Vercelプロジェクトメタ（後続再リンク参考）
```
projectId:    prj_aF4piF2XN9AfWdzzQNcu49ibUTDf
orgId:        team_lLlyCFuiOG3pLH4cA8J4iwHG
projectName:  quickry_book_lp
nodeVersion:  24.x        ← 新規作成時は 20.x へ切替推奨（後述 Step 4 根拠）
framework:    nextjs
```

### `.vercel/.env.production.local` 内容種別（秘密情報の有無）
キー名のみ列挙（実値は記載しない・絶対遵守2）。
```
NX_DAEMON / TURBO_*（5件） / VERCEL / VERCEL_ENV / VERCEL_GIT_*（10件） / VERCEL_OIDC_TOKEN /
VERCEL_TARGET_ENV / VERCEL_URL
```
- すべて Vercel CLI が自動生成する内部メタデータ
- `VERCEL_OIDC_TOKEN` のみ秘密情報相当だが `.gitignore` の `.env*` で git管理外
- **GA_ID 等のユーザー定義 env_var は含まれず** → Vercel Dashboard 側にのみ存在していた前提を確認

### 操作内容（破壊的操作なし）
```bash
mkdir -p ".../_archive/01_プロダクト_quickry_book_lp_vercel_old_20260510"
mv ".../01_プロダクト/quickry_book_lp/.vercel" \
   ".../_archive/01_プロダクト_quickry_book_lp_vercel_old_20260510/.vercel"
```
- `rm` 不使用・`mv` で退避のみ（絶対遵守3 セーフ）
- `_archive/` 配下は法定保存パス非該当（絶対遵守1 セーフ）
- `.gitignore` で `.vercel` 既載のためリポジトリには影響なし

---

## 3. Step 2 — コード整合性確認（詳細）

| ファイル | 期待状態 | 実状態 | 結果 |
|---------|---------|--------|------|
| `src/app/layout.tsx` | `metadataBase = https://lp-book.quickry.jp` / `canonical: "/"` 設定済 | line 21-23 で設定済 | ✅ |
| `src/app/robots.ts` | sitemap参照 `https://lp-book.quickry.jp/sitemap.xml` | 一致 | ✅ |
| `src/app/sitemap.ts` | `lp-book.quickry.jp` ベース・`/terms` `/privacy` `/legal` 含む | 4 URL 全件 `lp-book.quickry.jp` ベース | ✅ |
| `src/app/legal/` `privacy/` `terms/` ページ | 実装済 | 3ディレクトリ実在確認 | ✅ |
| `src/components/Footer.tsx` | 内部相対パス（`/terms` 等）or 外部URL | 内部相対パス（`/terms` `/privacy` `/legal`）で実装済 | ✅ |

**差分ファイル一覧**: なし（ゼロ件）。**修正不要**。

> 補足: 4/14 梶原CTO 手順書では Footer リンクを `about.quickry.jp/...` 外部URLにする案だったが、現状の実装では内部 `/legal` `/privacy` `/terms` ページを実装する形に進化しており整合がとれている。Phase 1（`lp-book.quickry.jp` 維持）ではこのままで問題なし。

---

## 4. Step 3 — env ファイル雛形作成（詳細）

### 配置
- `01_プロダクト/quickry_book_lp/.env.example`

### 内容
- `NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX`（placeholder のみ・実キーなし）
- 冒頭コメントで Vercel Dashboard 管理を明示

### git tracked 状態
- 現状の `.gitignore` は `.env*` を一律除外 → `.env.example` も対象（git tracked にならない）
- git tracked 化したい場合は梶原CTO承認の上で `.gitignore` に `!.env.example` 追記する必要（運用原則20）。本タスクでは未実施・要相談事項として残す（後述 Section 7）

---

## 5. Step 4 — package.json / Node Version 確認（詳細）

### Vercel 新規作成時の Node 推奨値: **20.x**

根拠:
| 項目 | 値 | 解釈 |
|------|-----|------|
| `package.json devDependencies @types/node` | `^20` | 開発時型定義は **Node 20.x ターゲット** で書かれている |
| `node_modules/@types/node` 実体 | `20.19.39`（^20 範囲内） | npm install 結果も 20.x |
| `package-lock.json` engines | `node >= 10`（依存パッケージ要求の最小値） | 上限制約なし・本体 engines 未指定 |
| `.nvmrc` | 不在 | Vercel Dashboard から明示指定が必要 |
| 旧 `.vercel/project.json` | `nodeVersion: 24.x` | **`@types/node ^20` と乖離**・ビルド時に型不整合の懸念 |

### 推奨アクション（坪井さん実操作）
- Vercel 新規プロジェクト作成時の **Node.js Version 設定で `20.x` を選択**（旧プロジェクトは 24.x だったが整合をとり 20.x に変更）

### 依存ツリー
- `next 16.2.3` / `react 19.2.4` / `tailwindcss 4.2.2` / `typescript 5.9.3` / `lucide-react 1.8.0`
- 6件 extraneous（@emnapi等の枝葉・Vercel ビルドには影響しない・無視可）
- セキュリティ脆弱性スキャンは Vercel ビルド時の Dependabot で別途検知される想定

---

## 6. 坪井CEO実操作待ち項目（再掲・親計画書 Section 3 と同一）

下記6項目すべて坪井さん実操作。AI側からは Vercel/AWS/Google Analytics の Dashboard 操作不可（絶対遵守4）。

- [ ] **A. Vercel 新規プロジェクト作成**（10分）
  - Vercel Dashboard → quickrys-projects チーム → Add New Project → Import `yusuketsuboi/quickry-book-lp`
  - プロジェクト名: `quickry-book-lp`（ハイフン区切り・旧名 `quickry_book_lp` とは別物）
  - **Node.js Version: `20.x` を選択**（旧24.xから変更・本報告 Step 4 で根拠提示）
  - 初回 Deploy 完了後、`*.vercel.app` URL と新 projectId を佐川（チャット）に共有
- [ ] **B. GA4 測定ID 取得**（5分）
  - Google Analytics → Quickry Book LP プロパティ → 測定ID `G-XXXXXXXXXX` をコピー
- [ ] **C. Vercel に環境変数追加**（5分）
  - Settings → Environment Variables → `NEXT_PUBLIC_GA_ID` = 上記測定ID（Production/Preview/Development 全て）
  - 追加後 Redeploy（Use existing Build Cache のチェックは外す）
- [ ] **D. Vercel カスタムドメイン追加**（5分）
  - Settings → Domains → `lp-book.quickry.jp` を Add → 表示された CNAME 値を佐川に共有
- [ ] **E. AWS Route 53 で CNAME レコード追加**（5分）
  - Hosted zones → `quickry.jp` → Create record
  - name=`lp-book` / type=CNAME / value=`cname.vercel-dns.com` / TTL=300
- [ ] **F. 公開後 GA4 計測確認**（5分・公開数時間後）
  - GA4 リアルタイムレポートで `lp-book.quickry.jp` のアクセス計測を確認

合計 35〜45分。

---

## 7. 残タスク・要相談事項

### 次セッションでの AI 実行待ち
- **Hero.tsx 編集**: 芝原コピー文言確定後（task4 連携）に別ターンで実装。本タスクでは対象外
- **DNS反映確認 (`dig` / `curl -I`)**: 坪井さん Step E 完了後に AI で実行
- **OGP 確認・PageSpeed Insights 計測・フォーム送信テスト**: 坪井さん Step A〜E 完了後に AI で実行
- **完了報告書作成**: 上記すべて完了後に `20260510_報告_LP復旧Phase1完了_佐川睦男.md`（または別日付）として正式版を作成

### 梶原CTO 要相談（次回レビュー時）
- `.env.example` の git tracked 化（`.gitignore` に `!.env.example` 追記）の可否
  - メリット: 新規参加エンジニアが env 変数の存在を把握しやすい
  - デメリット: `.env*` の一律除外ルールに例外を作る運用負荷
  - 現状: 未実施・本ファイルだけ作成（コミット時に明示的に Add する想定）

### 想定外シナリオ
- 旧 GA4 プロパティが見つからず新規作成する場合 → 既存 Quickry プロパティ（`about.quickry.jp` 等）のストリームを再利用するか別プロパティ立てるかの判断は坪井さん相談
- Vercel が DNS Configuration で CNAME ではなく A レコード（`76.76.21.21` 等）を要求する場合 → Route 53 でそちらを登録（CNAME と排他）

---

## セルフレビュー

**最新情報確認**:
- `.vercel/project.json` Read済（旧 projectId `prj_aF4piF2XN9AfWdzzQNcu49ibUTDf` / orgId `team_lLlyCFuiOG3pLH4cA8J4iwHG` / nodeVersion 24.x 確認）
- `.gitignore` Read済（`.vercel` `.env*` 既載確認）
- `src/app/layout.tsx` `robots.ts` `sitemap.ts` Footer.tsx Read済（梶原CTO 4/14手順書反映済確認）
- `package.json` `package-lock.json` Read済（@types/node ^20 / engines node >=10 / .nvmrc 不在）
- `src/app/` ディレクトリ list済（legal/privacy/terms 実在）
- `_archive/` 既存命名規則確認済（`01_プロダクト_xxx_YYYYMMDD` 形式）
- npm ls 実行済（依存ツリー 13件 + extraneous 6件）
- 親計画書 `20260509_計画_LP復旧Phase1_佐川睦男.md` 全件Read済
- 4/14 公開手順書 全件Read済
- AGENTS.md `01_プロダクト/quickry_book_lp/` 確認（Next.js は破壊的変更ありで `node_modules/next/dist/docs/` 参照必須・本タスクではコード書換なしのため該当なし）

**漏れ**:
- [自律実行済] 退避先パス補正（計画書 `../_archive/` → 実体不在判明→ マイドライブ直下 `_archive/` へ・既存4ディレクトリの命名規則と整合）
- [自律実行済] `.env.example` の git tracked 化検討（`.gitignore` `.env*` 一律除外と干渉する旨を雛形冒頭コメントと本報告書 Section 7 に明示・運用原則20の梶原CTO承認待ち事項として残置）
- [自律実行済] Vercel Node version 推奨値の根拠取得（旧24.x vs `@types/node ^20` の乖離を Step 4 で表化）
- [起票済] **次セッションでの AI 実行タスク**（DNS確認・PageSpeed・OGP・フォームテスト・完了報告書作成）→ 本報告書 Section 7 に列挙・坪井さん Step A〜E 完了後に着手
- [起票済] **Hero.tsx 編集**（task4 芝原文言確定後）→ 別ターン依頼として本報告書 Section 7 に明記

**最適化**:
- 現状: `.gitignore` の `.env*` 一律除外で `.env.example` 雛形も git管理外
- 改善案: `.gitignore` に `!.env.example` 1行追記して例外指定 → 新規エンジニアが env 変数の存在を把握可能
- 推奨理由: env 変数の知識共有コスト低減 + 雛形ファイルが秘密情報を持たない設計のため安全
- ステータス: [提案・要承認]（運用原則20「.gitignore 変更は梶原CTO承認必須」のため本タスクでは保留・梶原次回レビュー時に判断仰ぐ）

**波及**:
- `01_プロダクト/CLAUDE.md`（梶原CTO部門）: 復旧完了後に状態反映が必要だが本タスク完了時点では未反映（DNS・公開確認後に対応）
- `01_プロダクト/quickry_book_lp/`: `.vercel/` 退避により Vercel CLI からの操作は新規 link が必要な状態（坪井さん Step A 完了後に AI で `vercel link` または Dashboard 連携運用）
- `_archive/01_プロダクト_quickry_book_lp_vercel_old_20260510/`: 新規作成（既存4ディレクトリと整合）
- 親計画書: 本報告書から参照されるため移動・改名はしない方針

**次アクション**:
1. [要確認] 本報告書を坪井CEOに共有 → 坪井さん実操作6項目（A〜F）の着手タイミング確認
2. [自律] 坪井さん Step A（Vercel新規作成）完了通知後、`*.vercel.app` URL と新 projectId を受領し AI 側で動作確認（プレビュー HTTP 200 / OGP / GA4 計測）
3. [自律] 坪井さん Step E（Route 53 CNAME 追加）完了通知後、`dig +short lp-book.quickry.jp` `curl -I https://lp-book.quickry.jp` を AI 側で実行 → 反映確認
4. [自律] 全項目完了後、最終完了報告書（`20260510_報告_LP復旧Phase1完了_佐川睦男.md` または該当日付）を作成し梶原CTO・坪井CEOに報告
5. [要確認] `.env.example` の `.gitignore` 例外化を梶原CTO次回レビュー時に相談（運用原則20）
6. [要確認] Hero.tsx 編集の依頼（task4 芝原文言確定後の別ターン依頼）

**参照メモリ**:
- F02（feedback_no_inference）: GA_ID・Vercel projectId は推論せず坪井さん実操作待ちと明記
- F09（feedback_chat_paste_ready）: 坪井さん実操作手順は本報告書 Section 6 にチェックボックス形式で直記（チャット本文掲載は親 Agent 報告で実施）
- F26（feedback_verify_latest_state）: 着手前に対象ファイル全件Read・実体ディレクトリ確認・既存命名規則確認を完了
- F12（feedback_avoid_structure_bloat）: `.env.example` を新設するが `.gitignore` 改変は提案レベルで保留（仕組み増殖の事前審査で梶原CTO相談ルートに乗せる）
- 該当しない: その他

---

**以上**
