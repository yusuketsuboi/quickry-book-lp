# Quickry Book LP 復旧計画書（Phase 1）

訴求軸: 社内向け

- 作成日: 2026-05-09
- 作成者: 佐川睦男（AIエンジニア・LP/Web横断）
- レビュー想定: 梶原CTO
- 目的: `lp-book.quickry.jp` を 5/15 β版ローンチ前に再公開する
- スコープ: Phase 1 のみ（現行URL `lp-book.quickry.jp` 維持・LP内容刷新は対象外）
- 関連: `01_プロダクト/技術調査/20260414_LP公開手順書.md`（梶原CTO作成・基礎手順）

---

## 0. サマリ（坪井さん向け）

**結論**: 復旧自体は技術的に難しくない。ボトルネックは「旧Vercelプロジェクトの所有権」と「Route 53のDNS編集権限」の2点で、いずれも坪井さんの実操作が必要。

**所要時間の見積（想定どおり進んだ場合）**:
- 坪井さん実操作: 合計 30〜45分（分割可）
- 佐川（AI）実行: 合計 60〜90分（再リンク・コード差分確認・デプロイ確認・PageSpeed等）
- DNS反映待ち: 5分〜1時間（最大48時間・Route 53は通常数分）

**5/15ローンチ前完了見込み**: ◎（着手から半日〜1営業日以内で復旧可能）。坪井さんが今週中に下記「坪井CEO実操作項目リスト」を消化できれば確実。

**復旧スコープ外**:
- LP本文・デザイン刷新（川口CPO別タスク）
- `book.quickry.jp` への移行（Phase 2 / 6月以降）
- 法的ページ本文の更新（田島法務側で別途）

---

## 1. 現状確認（2026-05-09 時点）

### 1-1. Vercel プロジェクト
- 旧プロジェクトID: `prj_aF4piF2XN9AfWdzzQNcu49ibUTDf`
- 旧オーナー: `team_lLlyCFuiOG3pLH4cA8J4iwHG`（quickrys-projects と推定）
- 状態: `vercel ls` で「削除/移管/権限なし」表示。旧IDは復活させない方針（確認コストより新規作成の方が早い）

### 1-2. ドメイン
- `lp-book.quickry.jp`: DNS未配備（`Could not resolve host`）
- `quickry.jp` の権威DNS: **AWS Route 53**（NS が `ns-*.awsdns-*` で確定）
  - 既存稼働: `lp.quickry.jp` `about.quickry.jp` (162.43.101.142)・`www.quickry.jp` (CloudFront) は Route 53 経由
- 結論: DNSプロバイダ調査は完了。**Route 53 で確定**。お名前.com / Cloudflare の手順は本タスクでは不要。

### 1-3. GitHub
- リポジトリ: `https://github.com/yusuketsuboi/quickry-book-lp`（個人アカウント・private 想定）
- 直近コミット: `e8ea2ea chore: trigger deploy`（4/18 デプロイ試行）
- 4/14手順書では `quickry-inc/` とされていたが、実態は個人 `yusuketsuboi/`。Vercel Import 時はこのまま個人リポジトリを使う（移管は将来課題）

### 1-4. ローカル `.vercel/`
```
.vercel/
├── project.json            ← 旧 projectId / orgId（再リンク時に上書きされる）
├── .env.production.local   ← Vercel CLI 内部メタデータのみ。GA_ID等の自前 env_var は含まれない
├── README.txt
└── output/                 ← 過去の build artifact
```
GA_ID 等の **ユーザー定義環境変数は Vercel Dashboard 側にのみ存在** していたため、旧プロジェクト消失と同時に値は失われている前提（坪井さんに再取得を依頼）。

### 1-5. LPコード
- 4/14手順書のコード修正（`metadataBase` / `canonical` / `robots.ts` / `sitemap.ts`）はすでに反映済み（`src/app/layout.tsx`・`src/app/robots.ts`・`src/app/sitemap.ts`）
- `src/app/legal/page.tsx`・`privacy/page.tsx`・`terms/page.tsx` も実装済み → Footer リンクは内部相対パスでよい（外部URLへの差し替え不要）
- → **コード差分タスクはほぼ不要**。再リンクと再デプロイで通る想定。

---

## 2. 復旧手順（実行順）

### Step 1. 旧 `.vercel/` ディレクトリの取り扱い（AI実行）

旧 projectId が残っていると `vercel link` で誤誘導される恐れがあるため、安全な退避を行う。

```bash
cd "01_プロダクト/quickry_book_lp"

# 旧.vercel/ を退避（削除しない・万一の参照用に_archiveへ）
mv .vercel "../_archive/quickry_book_lp_vercel_old_20260509"

# .gitignore に .vercel/ が入っていることは確認済（Next.js標準）
```

> 補足: `.vercel/` は git 管理外（`.gitignore` 既載）なので、退避は完全にローカル作業。リポジトリには影響しない。

### Step 2. Vercel プロジェクト新規作成（坪井さん実操作）

> AIから Vercel API・Dashboard を直接操作することは絶対遵守4・運用方針上できないため、**Dashboard 操作は坪井さんに依頼**。

1. https://vercel.com/dashboard にログイン（quickrys-projects チーム選択）
2. 「Add New...」→「Project」
3. 「Import Git Repository」で `yusuketsuboi/quickry-book-lp` を選択
   - GitHub個人アカウント連携が必要なら、Vercel に GitHub アクセス許可（リポジトリ単体スコープでOK）
4. プロジェクト名: `quickry-book-lp`（ハイフン区切り。既存の旧プロジェクト名 `quickry_book_lp` とは別物として作成）
5. ビルド設定（**全てデフォルトのままでOK**）:
   | 項目 | 値 |
   |------|---|
   | Framework Preset | Next.js（自動検出） |
   | Root Directory | `.` |
   | Build Command | `next build` |
   | Output Directory | `.next` |
   | Install Command | `npm install` |
   | Node.js Version | `20.x` または `22.x`（旧は 24.x 設定。20.x 安定版を推奨） |
6. **環境変数は次の Step 3 で追加するので、ここではまだ Deploy しない方が無難**（`NEXT_PUBLIC_GA_ID` 未設定のままデプロイすると GA4 計測が抜ける）。一旦 Deploy しておき、Step 3 で値を入れたら Redeploy でも可。
7. 「Deploy」を押して、初回デプロイ完了後にプロジェクト URL（`xxx.vercel.app` 形式）をメモして佐川（チャット）に共有

**坪井さん→佐川 共有してほしい情報**:
- 新プロジェクトの URL（例: `quickry-book-lp.vercel.app`）
- Vercel が割り当てた projectId（Settings → General → Project ID）

### Step 3. 環境変数の設定（坪井さん実操作 + AI事前確認）

#### 3-1. GA4 測定IDの確認（坪井さん）

旧 `NEXT_PUBLIC_GA_ID` の値が失われている前提。以下を確認:

- Google Analytics（https://analytics.google.com/）にログイン
- 既存プロパティに「Quickry Book LP」があれば、**管理 → データストリーム → 測定ID（`G-XXXXXXXXXX`）** をコピー
- もし旧プロパティが残っていなければ、新規作成 or 既存「Quickry」プロパティのストリームを再利用するかを佐川に相談

#### 3-2. Vercel Dashboard で env_var 追加（坪井さん）

| Key | Value | Environment |
|-----|-------|-------------|
| `NEXT_PUBLIC_GA_ID` | `G-XXXXXXXXXX`（取得した測定ID） | Production / Preview / Development すべて |

> その他に必要な env_var は現状無し（Formspree は LP 内に直書き・サーバーサイド秘密キーは未使用）。

#### 3-3. Redeploy（坪井さんまたはAIどちらでも可）

- Dashboard の「Deployments」→ 最新デプロイの右端「⋯」→「Redeploy」（`Use existing Build Cache` のチェックは外す）

### Step 4. ローカル `.vercel/` 再リンク（AI実行）

新プロジェクト作成後、ローカルから `vercel` CLI で操作したい場合に必要。**坪井さんが自分でやらない限りはスキップ可**（Vercel は Git push トリガーで自動デプロイされるため、CLI からの操作は必須ではない）。

```bash
cd "01_プロダクト/quickry_book_lp"

# Vercel CLI ログイン（要 vercel login。坪井さんアカウント or 佐川作業用個別アカウント）
vercel link
# → Set up "quickry_book_lp"? Y
# → Which scope? quickrys-projects
# → Link to existing project? Y
# → What's the name of your existing project? quickry-book-lp
```

### Step 5. カスタムドメイン追加（坪井さん実操作 + AI確認）

#### 5-1. Vercel 側でドメイン登録（坪井さん）

1. Vercel プロジェクト → Settings → Domains
2. `lp-book.quickry.jp` を入力 → 「Add」
3. Vercel が表示する「DNS Configuration」セクションに以下のいずれかが出る:
   - **CNAME 推奨**: `cname.vercel-dns.com`（サブドメインの標準）
   - 稀に A レコード（`76.76.21.21` など）の指示が出る場合あり
4. 表示された値を佐川に共有（スクリーンショット推奨）

#### 5-2. AWS Route 53 で CNAME 追加（坪井さん実操作）

> **AI（佐川）は AWS Console 操作権限を保有していないため、坪井さんが Route 53 を直接操作する必要あり**。

1. AWS Console → Route 53 → Hosted zones → `quickry.jp` を選択
2. 「Create record」
3. 入力値:
   | 項目 | 値 |
   |------|---|
   | Record name | `lp-book` |
   | Record type | `CNAME` |
   | Value | `cname.vercel-dns.com`（Step 5-1 で Vercel が指示した値） |
   | TTL | 300（5分・反映を早めるため） |
   | Routing policy | Simple routing |
4. 「Create records」
5. 約5分〜1時間で反映（Route 53 は通常数分）

#### 5-3. 反映確認（AI実行）

```bash
# CNAME 反映チェック
dig +short lp-book.quickry.jp
# 期待値: cname.vercel-dns.com. が出る → さらに Vercel の IP に解決される

# HTTPS到達チェック
curl -I https://lp-book.quickry.jp
# 期待値: HTTP/2 200 (または初回は308リダイレクト → 200)
```

### Step 6. SSL 証明書発行確認（AI＋Vercel自動）

- Vercel が Let's Encrypt 経由で **自動発行**（DNS 反映後 5分〜数分）
- Vercel Dashboard → Domains で `lp-book.quickry.jp` のステータスが **「Valid Configuration」** になればOK
- 証明書発行が失敗する場合は CNAME 値を再確認（Cloudflareは未使用なので「橙色の雲」問題は発生しない）

### Step 7. 公開後動作確認（AI実行・坪井さんは結果報告だけ受領）

#### 7-1. 表示確認
- [ ] PC（1280px+）レイアウト崩れなし
- [ ] タブレット（768〜1279px）崩れなし
- [ ] スマホ（375〜767px）崩れなし
- [ ] スマホ最小（320px）崩れなし
- [ ] フローティングCTAがスクロール時に出る
- [ ] スクロールアニメーション動作

#### 7-2. OGP確認
- [ ] https://www.opengraph.xyz/ に `https://lp-book.quickry.jp` を入力 → タイトル・説明文・OGP画像（`/ogp.webp`）が正しく出る
- [ ] タイトル: `Quickry Book - 予約枠で、稼ぐ。`
- [ ] OGP画像: `https://lp-book.quickry.jp/ogp.webp` に解決される

#### 7-3. GA4動作確認
- [ ] Chrome DevTools → Network → `gtag` リクエスト送信を確認
- [ ] GA4 リアルタイムレポートでアクセスが計測される（坪井さんに GA画面確認を依頼）

#### 7-4. PageSpeed Insights
- [ ] https://pagespeed.web.google.com/ で `https://lp-book.quickry.jp` 計測
- [ ] モバイル: 90点以上
- [ ] PC: 95点以上
- [ ] LCP 2.5s 以内 / CLS 0.1 以内 / INP 200ms 以内

#### 7-5. フォーム送信テスト
- [ ] テストデータ（店舗名「テスト店舗_復旧確認_20260509」等識別可能な値）でフォーム送信 → Formspree 受信確認
- [ ] 完了メッセージ表示確認

---

## 3. 坪井CEO実操作項目リスト（チェックボックス形式）

復旧に必要な坪井さん作業を一覧化:

- [ ] **A. Vercel新規プロジェクト作成**（10分）
  - [ ] Vercel Dashboard ログイン
  - [ ] `yusuketsuboi/quickry-book-lp` を Import
  - [ ] プロジェクト名 `quickry-book-lp` で作成
  - [ ] 初回 Deploy 完了後、`*.vercel.app` URL を佐川に共有

- [ ] **B. GA4 測定ID 取得**（5分）
  - [ ] Google Analytics で「Quickry Book LP」プロパティの測定ID（`G-XXXXXXXXXX`）を確認
  - [ ] 旧プロパティが見当たらない場合は佐川に相談

- [ ] **C. Vercel に環境変数 `NEXT_PUBLIC_GA_ID` 追加**（5分）
  - [ ] Settings → Environment Variables → `NEXT_PUBLIC_GA_ID` = `G-XXXXXXXXXX` を Production/Preview/Development 全てに登録
  - [ ] Redeploy（cache off）

- [ ] **D. Vercel カスタムドメイン追加**（5分）
  - [ ] Settings → Domains → `lp-book.quickry.jp` を Add
  - [ ] 表示された CNAME 値（`cname.vercel-dns.com` 等）を佐川に共有

- [ ] **E. AWS Route 53 で CNAME レコード追加**（5分）
  - [ ] Hosted zones → `quickry.jp` 選択
  - [ ] Create record: name=`lp-book`, type=CNAME, value=`cname.vercel-dns.com`, TTL=300
  - [ ] Create records 押下

- [ ] **F. 公開後 GA4 計測確認**（5分・公開数時間後）
  - [ ] GA4 リアルタイムレポートで `lp-book.quickry.jp` のアクセスが計測されていることを確認

**合計 35〜45分**（タイミング分散可・連続実施なら30分前後）

---

## 4. AI（佐川）担当タスク

- [ ] 旧 `.vercel/` を `_archive/quickry_book_lp_vercel_old_20260509/` へ退避（5分）
- [ ] 新プロジェクト ID 受領後、`.vercel/` 再リンク（必要時のみ・5分）
- [ ] DNS反映 dig 確認（Step 5-3 / 5分）
- [ ] HTTPS到達 curl 確認（5分）
- [ ] OGP確認・スクリーンショット取得（10分）
- [ ] PageSpeed Insights 計測・スコア記録（10分）
- [ ] フォーム送信テスト（5分）
- [ ] 復旧完了報告書作成（`20260509_報告_LP復旧Phase1完了_佐川睦男.md`）（15分）

**合計 60〜90分**（坪井さん作業完了後に集中実施）

---

## 5. リスク・想定外事象への備え

| リスク | 対処 |
|--------|------|
| 旧 GA4 プロパティが残っており重複計測 | 旧プロパティを停止 or 新ストリームに統一（坪井さん判断） |
| Vercel 個人プラン制約で chime-domain 設定NG | quickrys-projects チームプランの状態を Settings → Billing で確認 |
| Vercel が表示する DNS 設定が CNAME ではなく A レコード（`76.76.21.21`）指示 | Route 53 で A レコードを追加（CNAMEと排他・どちらかを採用） |
| ビルドエラー（依存関係解決失敗） | `package-lock.json` ベースで `npm ci` 動作することを Vercel ローカル再現で確認 / Node 20 で安定 |
| Formspree フォーム ID `xpwryqqk` が無効化 | 佐川徳夫CMO に Formspree アカウント状況確認 |
| `lp-book.quickry.jp` 旧キャッシュで坪井さん環境からのみ閲覧不可 | DNS フラッシュ手順を案内（`sudo dscacheutil -flushcache` for macOS） |
| Phase 2 移行（`book.quickry.jp`）との重複稼働 | Phase 1 は `lp-book.quickry.jp` 維持で確定。Phase 2 切替時に 301 リダイレクト計画を別途策定 |

---

## 6. 5/15β版ローンチ前 完了見込み判定

| 項目 | 状態 |
|------|------|
| 技術的難易度 | 低（コード修正不要・新規作成のみ） |
| 坪井さん作業時間 | 35〜45分 |
| AI作業時間 | 60〜90分 |
| 物理的待ち時間 | DNS反映 5分〜1時間（Route 53は通常数分） |
| **総所要 | 半日〜1営業日** |
| **5/15ローンチ前完了見込み | ◎（今週中に坪井さん作業完了で確実）** |

**前提**:
- 坪井さん Vercel・GA4・Route 53 のログイン情報を保持していること
- quickrys-projects チームの Owner 権限を坪井さんが持っていること（持っていない場合、現 Owner の白石CTO等に Owner Transfer 依頼が必要）

---

## 7. 完了基準（DoD）

- [ ] `https://lp-book.quickry.jp` が HTTP 200 で表示される
- [ ] SSL 証明書が Vercel から自動発行され有効
- [ ] OGP 画像が SNS シェア時に正しく表示される
- [ ] GA4 リアルタイムでアクセスが計測される
- [ ] PageSpeed Insights モバイル90点以上
- [ ] フォーム送信が Formspree に届く
- [ ] 完了報告書を `20260509_報告_LP復旧Phase1完了_佐川睦男.md` で作成済み
- [ ] 梶原CTO・坪井CEO に完了報告

---

## 8. 次セッション・Phase 2 への引継ぎ

- Phase 2（6月以降）: `book.quickry.jp` 移行 + LP内容刷新
  - DNS 切替時の 301 リダイレクト戦略策定（`lp-book.quickry.jp` → `book.quickry.jp`）
  - SEO 影響最小化（canonical 切替 + Search Console プロパティ移行）
  - GitHub リポジトリを個人 `yusuketsuboi/` から組織アカウント（`quickry-inc/` 開設後）に移管検討
- LP 内容刷新は川口CPO別タスク（PRD ベース）

---

## セルフレビュー

**最新情報確認**:
- LP本体（`src/app/layout.tsx` Read済・metadataBase / canonical 反映済を確認）
- `.vercel/project.json` 確認済（旧 projectId / orgId 取得）
- `.vercel/.env.production.local` キー一覧のみ確認（値は Vercel CLI 内部メタのみで GA_ID 等は含まれず）
- DNS 権威確認（`dig NS quickry.jp` → AWS Route 53 確定）
- 既存サブドメイン解決確認（`lp.quickry.jp` 162.43.101.142 / `www.quickry.jp` CloudFront / `about.quickry.jp` 162.43.101.142）
- git remote 確認（`yusuketsuboi/quickry-book-lp` 個人アカウント・4/14手順書の `quickry-inc/` とは別）
- 既存legal/privacy/terms ページ実在確認（Footer 修正不要を確定）
- 4/14既存手順書 `01_プロダクト/技術調査/20260414_LP公開手順書.md` 全件Read済

**漏れ**:
- [自律実行済] DNSプロバイダ調査（Route 53 確定）→ お名前.com / Cloudflare 手順を計画書から省略しシンプル化
- [自律実行済] 既存legal/privacy/terms 内部実装確認 → Footer URL差替タスクを除外
- [要確認] quickrys-projects チームの Owner 権限を坪井さんが保有しているか（保有していない場合 Owner Transfer 依頼が前段に発生）→ 計画書 Section 6 末尾「前提」に明記済

**最適化**:
- 現状: 4/14手順書は3つのDNSプロバイダ（お名前.com / Cloudflare / Route 53）の手順を併記しており冗長
- 改善案: 復旧計画書では Route 53 単独に絞り、坪井さん操作の選択肢を最小化
- 推奨理由: 認知負荷低減 + 操作ミス削減（[自律実行済]）

**波及**:
- 既存4/14手順書（梶原CTO作成）: 内容更新は不要（基礎手順として温存・本計画書から参照）
- `01_プロダクト/quickry_book_lp/.vercel/`: 退避操作は実行ステップ Step 1 で AI 実行予定（本計画承認後）
- `01_プロダクト/CLAUDE.md`（梶原CTO）: 復旧完了後の状態反映が必要だが本タスク完了時に対応
- 5/15β版ローンチスケジュール: 影響なし・むしろ前進

**次アクション**:
1. [要確認] 本計画書の方針（Route 53 単独・新Vercelプロジェクト名 `quickry-book-lp`・GitHub個人アカ継続）について坪井CEO・梶原CTO に承認依頼
2. [自律] 承認後、Step 1（旧 `.vercel/` 退避）を即実行
3. [要確認] Step 2〜5（坪井さん実操作）の着手タイミング調整
4. [自律] 坪井さん完了後、Step 6〜7 のAI動作確認を即実行し復旧完了報告書を作成

**参照メモリ**:
- F02（feedback_no_inference）: GA_ID 値は推論せず「失われている前提・坪井さんに再取得依頼」と明示
- F09（feedback_chat_paste_ready）: 本計画書はチャット本文ではなくファイル成果物だが、坪井さん操作手順は逐次コードブロック・コマンド直記
- F26（feedback_verify_latest_state）: 着手前に dig / git / ファイルRead で現在値確認済
- 該当しない: その他

---

**以上**
